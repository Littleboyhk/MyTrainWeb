# Public site URL used for metadata / Open Graph tags.
# No domain registered yet — set this to your Vercel preview/prod URL.
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# --- Supabase (wire up later) ---
# The waitlist API route (app/api/waitlist/route.ts) is scaffolded to insert
# into your existing Supabase project. Fill these in when ready.
# NEXT_PUBLIC_SUPABASE_URL=your-project-url
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key   # server-only, never expose to client
# SUPABASE_WAITLIST_TABLE=waitlist
